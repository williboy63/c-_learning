#include <iostream>
#include <cmath>


int main()
{
    //std::cout << sqrt(25) << std::endl; //Square Root function (#include <cmath>)

    //std::cout << pow(9, 99999999999999999999) << std::endl; //This is infinity
    //std::cout << INFINITY << std::endl;

    //std::cout << pow(-9, 99999999999999999999) << std::endl; //This is minimum infinity
    //std::cout << -INFINITY << std::endl; 

    //std::cout << remainder(10, 3) << std::endl; // This is equal to std::cout << 10%3 << std::endl;
    //If you what do do a remainder with a float, use de function, not the operator

    //std::cout << fmax(10, 3) << std::endl; //Gives the MAX argument; fmin gives the MIN

    //std::cout << ceil(fmin(10, 3.25)) << std::endl; //ceil() will round up (here is gonna be 4); floor() will round down (3)

    //std::cout << floor(-1.5) << std::endl; //This will round down to -2
    //std::cout << trunc(-1.5) << std::endl; //This will remove anything after decimal (-1)

    //std::cout << round(-1.4) << std::endl; //This will actually round the value (-1)

}